
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("СкладДляОтбора") Тогда
		СкладДляОтбора = Параметры.СкладДляОтбора;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура Создать(Команда)
	
	ПараметрыФормы = Новый Структура("СкладДляОтбора", СкладДляОтбора);
	ОткрытьФорму("РегистрСведений.гиг_СоответствиеСкладовСхемамОбеспечения.ФормаЗаписи", ПараметрыФормы, ЭтаФорма);

КонецПроцедуры


