
&Вместо("ЗаполнитьПараметры")
Процедура Рин1_ЗаполнитьПараметры(Знач ОбластьПечатнойФормы, Знач ДанныеОбъекта)
	
	Если ДанныеОбъекта = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Для Каждого ПараметрЗначение Из ДанныеОбъекта Цикл
		Если ТипЗнч(ПараметрЗначение.Значение) <> Тип("Массив") Тогда
			Заменить(ОбластьПечатнойФормы.Document.Content, ПараметрЗначение.Ключ, "");
		КонецЕсли;
	КонецЦикла;

	
КонецПроцедуры
