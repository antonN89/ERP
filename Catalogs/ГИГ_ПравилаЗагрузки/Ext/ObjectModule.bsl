
Процедура ПередЗаписью(Отказ)
	
	ТекстОшибки = НСтр("ru='Ни одно из полей значения тарифа (с НДС или без НДС) не заполнено'");
	Если НЕ ЭтоГруппа Тогда 
		Если Не ЗначениеЗаполнено(КолонкаЗначениеТарифа) и  Не ЗначениеЗаполнено(КолонкаЗначениеТарифаСНДС) Тогда
			ТекстСообщения = ТекстОшибки;
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстСообщения,,"КолонкаЗначениеТарифа",,Отказ);
			Отказ = Истина; 
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

